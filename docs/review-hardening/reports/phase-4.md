# Phase 4 — decision, execution and failure boundaries

Status: passed. Start HEAD and end HEAD remain `d48d327c7a6836dbe11b3d331b49420b4bb1058c` on `codex/review-hardening-0.92.0`; the task working tree is dirty and uncommitted. Phase 3 final fingerprint was `88c08e6943efca951dd8e1b237f78c8e09ed5e33948e1ec06156c273e84881a2`. Exact commands, execution environment, start/end dirty lists and code fingerprints are retained in `../evidence/phase-4-*.json`. Raw logs and frozen snapshots are at `/tmp/click-review-hardening-20260908/`.

## Prepare and reuse

The route is Hook state lock → source/revision/authority validation → current workspace/environment/executable capture → exact/dependency/safe-change decision → tentative source promotion → common final input confirmation → incremental plan → state save → reuse-only response or one-use runner issuance. Safe-change's existing per-decision snapshot remains in place. All three reuse modes now also pass one common fresh workspace and executable/environment collection before plan/storage. A first execution without reusable evidence takes no additional reuse snapshot.

The deterministic test seam calls the real matcher or promotion and changes fixture inputs just after it returns success. It exposes no new runtime API. On the frozen Phase 3 source, 14 subcases across 4 methods returned a reuse-only command after file modify/create/delete/rename/shared-config/lockfile or environment changes. Three further tests reproduced symlink target replacement, executable replacement outside the workspace, and a change after safe-change promotion. These are reproduced stale-input acceptance gaps at the tested scheduling points, not a demonstrated remote exploit.

The common check invalidates affected tentative reuse, restores pre-promotion facts and counters, and uses the established real-run path. Workspace drift uses the same existing revision/external-evidence invalidation logic extracted into one local helper. Unresolvable executable authority returns an explicit error. Execution-environment-only drift uses existing canonical reason codes. The same final fixture is also checked with its complete original command: failing changed content fails in both the Click run and the direct control; deletion/rename scenarios pass in both. This is an experiment audit, not new production work and not a proof of test sufficiency.

Prepare regressions: `phase-4-prepare-drift-green-fixed` passed the two workspace methods (12 subcases); its two environment cases exposed an implementation reason-code mismatch and did not pass. `phase-4-environment-diagnose` confirmed explicit plan rejection. After using existing binding reason codes, `phase-4-environment-green` passed 2/2. `phase-4-late-bindings-green` passed 3/3. The earlier `phase-4-prepare-drift-green` did not import because the initial extraction split at a nested else; this edit mistake was corrected before any test ran. Logs retain these intermediate failures. Final distinct coverage is 7 methods / 17 injected scenarios, all passing, no Linux skips.

## Claims and result storage

Claim validates current revision with the same strict owner helper. An invocation-local `_click_claim_binding` carries existing mode/status, contract digest, revision and current task/approval identity; it is never written into stored schema or public receipts. Result recording and the first/second collection-boundary reads compare against this original claim, preserving one-use token/claimed-ledger checks. This closes the reproduced malformed-after-prepare and changed-after-claim cases. Normal lifecycle cancellation already replaces/revokes tokens. Trusted-state fixture corruption is distinguished from normal APIs and is not an external injection feature.

`phase-4-boundary-red` ran 9 methods against a frozen pre-change source: 25 failing subtests and 12 error subtests exposed invalid current revision, changed status/contract and the second authority read. `phase-4-boundary-green` passed 9/9 after the change. The later collection-origin check required one explicit expected-claim keyword at the existing runner callsite; that follow-up is recorded in `../evidence/phase-4-collection-claim-red.json`, with final green coverage in `../evidence/phase-4-boundary-final.json` and `../evidence/phase-4-boundary-identity-final.json`. `phase-4-task-identity-red` reproduced 4 same-digest identity changes before those existing identity fields were included. Final `phase-4-boundary-identity-final` passed 21/21 (10 new boundary methods, 9 failure-collection cases, 2 normal Guarded/safe-shard cases), skip 0, exit 0, 30.884511 seconds, fingerprint `76e67097346357cebc5b8be5cc9e5f6ff0183a64c91033f91d3b0f0a393dcf09` unchanged.

Primary claim-write failure, primary result-write failure and secondary recovery-copy failure were already fail-closed in the tested paths. They received deterministic tests, with no storage implementation change. A failed atomic primary write and a successful primary commit followed by recovery failure are distinct states and are not described as one transaction.

## Process termination

A ready-pipe Linux subprocess probe reproduced three survivor cases in explicit terminate/timeout handling: leader already exited, leader exits after SIGTERM while child ignores it, and run_argv PIPE timeout with exited leader. The fixture excludes zombies when checking survival and reaps all test descendants. The POSIX terminate branch now signals the original isolated process group even after the leader exits, checks for a remaining group through the grace period, and escalates to SIGKILL if necessary. `phase-4-process-unit-red` reproduced all 3 new tests failing. `phase-4-process-unit-green` passed all 16 process tests, including existing Windows mocks, exit 0, skip 0, 5.368548 seconds. Normal successful detached-command policy is outside this change. Native Windows/macOS execution remains unverified. Children that create a separate session are outside the same-group boundary; zombie-only groups can consume the remaining grace interval.

## Existing protections and limits

`phase-4-boundary-regression` passed 22 existing checks, including one-use replay, altered tokens, cancellation/late completion, reservation TTL, non-expiring claimed runs, interrupted runs, result binding loss, source-boundary drift, state recovery, partial sibling retry, successor partial code reuse, per-shard safe-change and executable checks. It preserved normal partial reuse while refusing the tested invalid states.

The state lock serializes Click state, not external user edits. Filesystem snapshots and environment/executable collections cannot make all external inputs atomic; changes after the final collection remain a boundary limitation. Inputs outside supported observation coverage remain non-authoritative. Linux execution does not establish native Windows/macOS observer or process guarantees. Final full regression and generated distribution parity are reserved for Phase 6.

## Handoff

Phase 4 is locally passed. The final focused fingerprints differ because agents edited separate tests and process/verification regions during the phase; exact per-command values are preserved. Final full-suite validation will use one settled source. Phase 5 is restricted to fallback JSON-report retention and resource use. The new successor initializer explicitly rejects non-string kinds before membership validation; this is a defensive type-contract check, with normal initializer behavior unchanged. No release/remote mutation occurred.

The new ReviewHardeningGateTests class was later moved without assertion changes from test_click_review_hardening.py into existing test_click_gate_verification.py. Earlier log selectors intentionally describe the source at their recorded fingerprints. Final CI discovers the same methods under tests.test_click_gate_verification.ReviewHardeningGateTests; the repository's user-owned shard/reuse policy files remain unchanged.

The two existing POSIX mock tests retain their exact 0.25-second timeout assertions by using a fixed monotonic clock, rather than broadening the expected timeout range. `phase-4-process-exact-clock` passed 2/2, exit 0. The signal assertion now explicitly includes the additional group-existence probe. No existing assertion was deleted to make the implementation pass.
